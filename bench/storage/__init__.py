"""Storage benchmarks (a category): `transfer_speed` (s5cmd -> S3 write ceiling), plus future read/listing
benches. Each is a lean script sharing `constants.py`. Run e.g. `uv run -m bench.storage.transfer_speed`."""
